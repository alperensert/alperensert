```http
GET /alperensert HTTP/2
Host: github.com
Accept: application/json
```

```http
HTTP/2 200 OK
Content-Type: application/json
X-Powered-By: mass amounts of coffee
```

```json
{
  "role": "backend developer",
  "focus": "scalable systems, APIs, developer tools",
  "principle": "efficient, clean code — minimal allocations, no unnecessary abstractions",
  "stack": {
    "languages": ["C#", "TypeScript"],
    "backend": ["ASP.NET", "EF Core", "Dapper", "gRPC"],
    "data": ["PostgreSQL", "MSSQL", "Redis", "MongoDB"],
    "infra": ["Docker", "NATS", "Nginx", "Linux"]
  },
  "links": {
    "web": "https://alperen.io"
  }
}
```
